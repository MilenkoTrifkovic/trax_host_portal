import 'package:flutter/material.dart';

/// This widget is no longer used as sign-up functionality has been removed.
/// Only host users can log in to this portal and they are created by administrators.
class SignInToggle extends StatelessWidget {
  const SignInToggle({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
